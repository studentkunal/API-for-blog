<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Blog API</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(to right, #fdfcfb, #e2d1c3);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .container {
      display: flex;
      background-color: #fff;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
      border-radius: 15px;
      overflow: hidden;
      max-width: 950px;
      width: 100%;
    }

    .image-side {
      flex: 1;
      background: url('https://images.unsplash.com/photo-1515378791036-0648a3ef77b2') no-repeat center center/cover;
    }

    .login-box {
      flex: 1;
      padding: 60px 50px;
      box-sizing: border-box;
      background: #fffdf8;
    }

    .login-box h2 {
      text-align: center;
      color: #333;
      margin-bottom: 30px;
      font-size: 28px;
    }

    .login-box input[type="text"],
    .login-box input[type="password"] {
      width: 100%;
      padding: 15px;
      margin: 12px 0;
      border: 1px solid #ddd;
      border-radius: 10px;
      font-size: 16px;
      background-color: #fffaf3;
    }

    .login-box button {
      width: 100%;
      padding: 15px;
      background: #ff8c42;
      color: white;
      border: none;
      border-radius: 10px;
      font-size: 16px;
      cursor: pointer;
      margin-top: 20px;
      transition: background 0.3s;
    }

    .login-box button:hover {
      background: #e6762f;
    }

    .footer {
      text-align: center;
      margin-top: 20px;
      color: #777;
    }

    .footer a {
      color: #ff8c42;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="image-side"></div>
    <div class="login-box">
      <h2>Welcome Back Blogger ✍️</h2>
      <form action="/login" method="POST">
        <input type="text" name="username" placeholder="Username" required />
        <input type="password" name="password" placeholder="Password" required />
        <button type="submit">Login</button>
      </form>
      <div class="footer">
        Don’t have an account? <a href="/register">Register</a>
      </div>
    </div>
  </div>
</body>
</html>


<!-- <html>
<head>
  <title>Login</title>
</head>
<body>
  <h1>Login</h1>
  <% if (error) { %>
    <p style="color: red;"><%= error %></p>
  <% } %>
  <form action="/login" method="POST">
    <input type="text" name="username" placeholder="Username" required>
    <br>
    <input type="password" name="password" placeholder="Password" required>
    <br>
    <button type="submit">Login</button>
  </form>
</body>
</html> -->